# Copie para .env (gitignored) e ajuste se quiser.
# Valores default servem para o ambiente de dev local em Docker —
# NÃO usar estas senhas em qualquer ambiente exposto.
GLPI_DB_NAME=glpi
GLPI_DB_USER=glpi
GLPI_DB_PASSWORD=glpi
GLPI_DB_ROOT_PASSWORD=glpi_root
